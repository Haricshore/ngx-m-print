import { NgxMPrintButtonDirective } from './ngx-m-print.directive';

describe('PrintDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMPrintButtonDirective();
    expect(directive).toBeTruthy();
  });
});
